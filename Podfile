source 'https://github.com/CocoaPods/Specs.git'

platform :ios, '9.0'

# flag makes all dependencies build as frameworks
use_frameworks!

abstract_target 'FrontPageBase' do

   pod 'Apollo', '~> 0.6.0'

   target 'FrontPage' do
   end
end
